# i18n (7-language) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the koikatsu-dedupe GUI fully multilingual (繁中/简中/en/ja/ko/ru/es) with a runtime language switcher that auto-detects on first run and remembers the choice.

**Architecture:** A single dependency-free `dist/i18n.js` holds all 7 dictionaries plus a tiny runtime (`t()`, plural rules, language detection). `dist/index.html` marks static nodes with `data-i18n*` attributes and routes every dynamic string through `t()`. Two hand-written Chinese errors in the Rust backend become stable codes the frontend localizes. A Node parity script guards translation completeness.

**Tech Stack:** Vanilla JS (no framework, no bundler, no build step), Tauri 2 (Rust), Node (only for the zero-dep test scripts).

## Global Constraints

- **No new runtime dependencies. No build step. No bundler.** (Project core value — README: "plain HTML/CSS/JS — no build step".)
- Reference / fallback language is **`zh-Hant`** — its strings are the *verbatim* current shipping strings.
- Final detection fallback is **`en`**.
- `dist/i18n.js` must load under both browser (`file://`, Tauri) and Node `vm` (for tests) — reference only `globalThis`, never `window`/`document`/`navigator` at module top level.
- Language list & order (native names): `zh-Hant 繁體中文`, `zh-Hans 简体中文`, `en English`, `ja 日本語`, `ko 한국어`, `ru Русский`, `es Español`.
- Plural keys use **objects in every language**; ru must define `one/few/many`; en & es must define `one/other`; zh-Hant/zh-Hans/ja/ko define `other`. Plural category is chosen from `params.n`.
- Persisted language key: `localStorage['dedupe.lang']`.
- Keep `cargo test` green; the only Rust change is two error-string literals → codes.

---

## File Structure

- **Create** `dist/i18n.js` — all dictionaries + runtime (`I18N`, `LANGS`, `PLURAL_KEYS`, `detectLang`, `setLang`, `getLang`, `t`, `pluralCategory`). Single responsibility: translation data + lookup.
- **Create** `scripts/i18n.test.mjs` — unit tests for the runtime (detect/interpolate/plural/fallback).
- **Create** `scripts/check-i18n.mjs` — parity validator across all languages.
- **Modify** `dist/index.html` — `<script src>`, `data-i18n*` markup, switcher, init/apply, dynamic `t()` calls.
- **Modify** `src-tauri/src/lib.rs` — two error literals → codes.
- **Modify** `src-tauri/capabilities/default.json` — add `core:window:allow-set-title`.
- **Modify** `src-tauri/tauri.conf.json` — neutral window title fallback.

---

## Task 1: i18n runtime + zh-Hant reference dictionary + test harness

**Files:**
- Create: `dist/i18n.js`
- Create: `scripts/i18n.test.mjs`
- Create: `scripts/check-i18n.mjs`

**Interfaces:**
- Produces (all on `globalThis`):
  - `I18N: { [code]: { [key]: string | {one?,few?,many?,other?} } }`
  - `LANGS: Array<{code:string, native:string}>`
  - `PLURAL_KEYS: string[]`
  - `detectLang(tag:string): string`
  - `setLang(code:string): string` (sets internal `_lang`, ignores unknown→REF, returns active)
  - `getLang(): string`
  - `t(key:string, params?:object): string`
  - `pluralCategory(lang:string, n:number): 'one'|'few'|'many'|'other'`

- [ ] **Step 1: Write the failing runtime test**

Create `scripts/i18n.test.mjs`:

```js
import { readFileSync } from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const code = readFileSync(join(here, '..', 'dist', 'i18n.js'), 'utf8');
const sb = {};
vm.createContext(sb);
vm.runInContext(code, sb); // i18n.js targets globalThis → assigns onto sb

const { detectLang, setLang, t, pluralCategory } = sb;

// detection
assert.equal(detectLang('zh-TW'), 'zh-Hant');
assert.equal(detectLang('zh-Hant-HK'), 'zh-Hant');
assert.equal(detectLang('zh-CN'), 'zh-Hans');
assert.equal(detectLang('zh'), 'zh-Hans');
assert.equal(detectLang('en-US'), 'en');
assert.equal(detectLang('ja'), 'ja');
assert.equal(detectLang('ko-KR'), 'ko');
assert.equal(detectLang('ru'), 'ru');
assert.equal(detectLang('es-419'), 'es');
assert.equal(detectLang('fr-FR'), 'en'); // fallback
assert.equal(detectLang(''), 'en');

// interpolation + number local/escape-free
setLang('zh-Hant');
assert.equal(t('sync.log_error'), '錯誤：');
assert.equal(t('review.group_count', { i: 1, n: 20 }), '第 1 組 / 共 20 組');

// fallback to reference when key missing in a language
setLang('en');
assert.equal(t('__definitely_missing__'), '__definitely_missing__'); // last resort = key

// plural categories
assert.equal(pluralCategory('en', 1), 'one');
assert.equal(pluralCategory('en', 2), 'other');
assert.equal(pluralCategory('es', 1), 'one');
assert.equal(pluralCategory('ru', 1), 'one');
assert.equal(pluralCategory('ru', 2), 'few');
assert.equal(pluralCategory('ru', 5), 'many');
assert.equal(pluralCategory('ru', 21), 'one');
assert.equal(pluralCategory('ru', 11), 'many');
assert.equal(pluralCategory('ja', 5), 'other');

console.log('i18n.test.mjs OK');
```

- [ ] **Step 2: Run it to verify it fails**

Run: `node scripts/i18n.test.mjs`
Expected: FAIL — `ENOENT` / cannot read `dist/i18n.js` (file not created yet).

- [ ] **Step 3: Create `dist/i18n.js` runtime + zh-Hant dictionary**

Create `dist/i18n.js`. Runtime first:

```js
// dist/i18n.js — zero-dependency runtime i18n. Loaded BEFORE the main <script>.
// Browser: assigns onto window. Node (tests): assigns onto the vm context globalThis.
(function (g) {
  'use strict';
  const REF = 'zh-Hant';
  let _lang = REF;

  const LANGS = [
    { code: 'zh-Hant', native: '繁體中文' },
    { code: 'zh-Hans', native: '简体中文' },
    { code: 'en',      native: 'English' },
    { code: 'ja',      native: '日本語' },
    { code: 'ko',      native: '한국어' },
    { code: 'ru',      native: 'Русский' },
    { code: 'es',      native: 'Español' },
  ];

  // keys whose value is a plural object in EVERY language; category from params.n
  const PLURAL_KEYS = [
    'setup.avail', 'review.sel_info', 'detail.tally_same', 'detail.tally_diff',
    'summary.total', 'toast.deleted', 'toast.deleted_size', 'toast.delete_errors',
  ];

  function detectLang(tag) {
    const s = String(tag || '').toLowerCase();
    if (s === 'zh' || s.startsWith('zh-') || s.startsWith('zh_')) {
      if (s.includes('hant') || /^zh[-_](tw|hk|mo)\b/.test(s)) return 'zh-Hant';
      return 'zh-Hans';
    }
    for (const code of ['en', 'ja', 'ko', 'ru', 'es']) {
      if (s === code || s.startsWith(code + '-') || s.startsWith(code + '_')) return code;
    }
    return 'en';
  }

  function setLang(code) { _lang = I18N[code] ? code : REF; return _lang; }
  function getLang() { return _lang; }

  function pluralCategory(lang, n) {
    n = Math.abs(Number(n) || 0);
    if (lang === 'ru') {
      const a = n % 10, b = n % 100;
      if (a === 1 && b !== 11) return 'one';
      if (a >= 2 && a <= 4 && (b < 12 || b > 14)) return 'few';
      return 'many';
    }
    if (lang === 'en' || lang === 'es') return n === 1 ? 'one' : 'other';
    return 'other';
  }

  function rawLookup(lang, key) {
    const d = I18N[lang];
    if (d && Object.prototype.hasOwnProperty.call(d, key)) return d[key];
    const r = I18N[REF];
    if (r && Object.prototype.hasOwnProperty.call(r, key)) return r[key];
    return key; // last resort — never throw
  }

  function t(key, params) {
    params = params || {};
    let val = rawLookup(_lang, key);
    if (val && typeof val === 'object') {
      const cat = pluralCategory(_lang, params.n);
      val = (cat in val) ? val[cat] : (val.other != null ? val.other : val.one);
    }
    return String(val).replace(/\{(\w+)\}/g, function (_m, name) {
      const v = params[name];
      if (v == null) return '';
      if (typeof v === 'number') { try { return v.toLocaleString(_lang); } catch (e) { return String(v); } }
      return String(v);
    });
  }

  const I18N = {
    'zh-Hant': {
      'app.title': '卡片去重',
      'help.btn_title': '這是什麼？怎麼運作？',
      'step.sync': '1 整理卡片',
      'step.setup': '2 選一批',
      'step.review': '3 挑重複',
      'step.delete': '4 刪除',

      'sync.title': '整理卡片（建一份清單）',
      'sync.sub': '先掃一次你的卡片資料夾、記下每張卡的特徵，之後找重複都用這份清單，不必每次重掃。',
      'sync.label.root': '卡片資料夾',
      'sync.browse': '瀏覽…',
      'sync.label.db': '索引檔',
      'sync.pick_loc': '選位置',
      'sync.label.game': '遊戲路徑',
      'sync.game_ph': '（選填）例：…\\Koikatsu\\UserData\\chara\\female，用於「複製到遊戲目錄」',
      'sync.pick_folder': '選資料夾',
      'sync.label.mode': '找重複的方式',
      'sync.label.rebuild': '重建索引',
      'sync.rebuild_opt': '重新建立整份清單',
      'sync.recent': '最近：',
      'sync.recent_load': '點擊載入',
      'sync.idle': '待命中',
      'sync.log_start': '按「開始整理」啟動…',
      'sync.start': '開始整理',
      'sync.skip': '下一步 →',
      'sync.root_ok': '✓ 資料夾存在 · {n} 個 png',
      'sync.root_bad': '✗ 找不到 png · {n} 個 png',
      'sync.computing': '計算中…',
      'sync.starting': '開始整理{full}…',
      'sync.full_note': '（重建整份清單）',
      'sync.log_starting': '開始整理…',
      'sync.log_done': '完成 · 共 {total} 張，找到 {groups} 組重複（可清 {dup} 張）；新增 {added}、移除 {pruned}',
      'sync.done': '✓ 整理完成',
      'sync.log_error': '錯誤：',
      'sync.error': '✗ 出錯了',

      'mode.byte': '完全一樣的檔案',
      'mode.char': '同角色、不同封面',
      'mode.byte_short': '完全一樣',
      'mode.char_short': '同角色不同封面',

      'phase.scan': '掃描卡片',
      'phase.compare': '比對中',
      'phase.read_char': '讀取角色資料',

      'setup.title': '選一批來看',
      'setup.sub': '從剛剛那份清單，把重複的卡撈出來處理。一次看幾組由你決定。',
      'setup.byte_desc': '同一張卡被複製或改名存了好幾份，內容一模一樣，留一張即可。',
      'setup.char_desc': '只看卡片裡的角色資料、不管封面圖，所以換了封面的同一個角色也抓得到。',
      'setup.badge_ignore_cover': '無視封面',
      'setup.batch_label': '一次看幾組',
      'setup.avail': { other: '／ 可用 {n} 組' },
      'setup.avail_not_synced': '／「{label}」還沒整理過 — 回上一步跑一次',
      'setup.avail_none': '／ 還沒整理',
      'setup.start': '開始挑 →',

      'common.back': '← 回上一步',

      'review.group_count': '第 {i} 組 / 共 {n} 組',
      'review.badge_char': '同一個角色 · 封面不同',
      'review.title_byte': '這幾張是一模一樣的檔案',
      'review.title_char': '這幾張其實是同一個角色',
      'review.hint': '點一下你<b style="color:var(--hl)">想刪掉</b>的卡（會亮黃框），沒點的會留著。挑好按「下一組」。（數字鍵 1–9 快速選、← → 換組）',
      'review.copied': '✓ 已複製',
      'review.copy_to_game': '複製到遊戲目錄',
      'review.copied_btn': '已複製 ✓',
      'review.select_others': '留第一張、其餘都選',
      'review.detail': '看裡面內容',
      'review.auto': '⚡ 全部交給它',
      'review.sel_info': { other: '已選 {n} 張要刪' },
      'review.prev': '← 上一組',
      'review.next': '下一組 →',

      'detail.loading': '載入角色資料中…',
      'detail.read_fail': '(讀取失敗) ',
      'detail.rescan': '▶ 逐行比對',
      'detail.same': '相同',
      'detail.diff': '不同',
      'detail.tally_same': { other: '{same} 列相同' },
      'detail.tally_diff': { other: '{diff} 列不同' },

      'summary.title': '最後確認',
      'summary.sub': '這些是你標記要刪的卡。確認後才會真的刪 —— 本機進系統回收桶、NAS 進 NAS 回收桶，都救得回來。',
      'summary.total': { other: '共 {count} 張，約 {size}' },
      'summary.warn': '⚠ 這是「同角色、不同封面」的卡 —— 被刪那幾張的<b>封面會消失</b>（仍可從回收桶復原）。',
      'summary.col_group': '組',
      'summary.col_name': '檔名',
      'summary.col_size': '大小',
      'summary.delete': '確認刪除',
      'summary.back': '← 回去再看',

      'toast.deleted': { other: '已刪除 {n} 張 ✓' },
      'toast.deleted_size': { other: '已刪除 {n} 張（{mb} MB）' },
      'toast.delete_errors': { other: '（{n} 個錯誤）' },
      'toast.none_selected': '沒有選取任何卡',
      'toast.delete_fail': '刪除失敗：{e}',
      'toast.copy_desktop_only': '複製到遊戲目錄僅桌面版可用',
      'toast.set_game_path': '先在步驟1填「遊戲 chara 路徑」',
      'toast.no_src_path': '此卡無來源路徑',
      'toast.copied_ok': '已複製到遊戲目錄 ✓',
      'toast.copy_fail': '複製失敗：{e}',
      'toast.read_fail': '讀取失敗：{e}',
      'toast.no_dups': '找不到重複的卡，先回上一步「整理卡片」跑一次',
      'toast.auto_desktop_only': '全部自動：桌面版功能',
      'toast.pick_root_first': '先選卡片資料夾',

      'err.src_no_name': '來源路徑無檔名',
      'err.game_path_invalid': '遊戲 chara 路徑不存在或不是資料夾：{path}',

      'help.title': 'How it works',
      'help.subtitle': '· 技術細節',
      'help.lead': '兩種比對模式：<b>全圖 hash</b> 與 <b>只比對角色資料區塊</b>，共用一張 SQLite 索引表做增量更新。',
      'help.h_normal': '一般模式 · 全圖 hash（找一模一樣的檔案，快速跳過明顯不一致的）',
      'help.p_normal': '先比對檔案大小，size 不一樣的跳過；再比前 1 MB 的 hash，都一樣才跑完整檔 hash（都用 <code>xxHash64</code>）。',
      'help.h_advanced': '進階模式 · 只比角色資料區塊（找同角色、不同卡面，無視卡面差異）',
      'help.p_advanced': '走 chunk 到 <code>IEND</code>，只 hash 後面那段角色資料、卡面不管。先比長度（<code>char_len</code>）跳掉不一樣的，再比內容（<code>char_hash</code>）分組。純位元組、不解碼字串 → 非 UTF-8 角色名也安全。',
      'help.h_build': '怎麼建表',
      'help.p_build': '掃描頂層 PNG → 寫進 SQLite 單表 <code>files(path PK, size, mtime, head_hash, full_hash, char_len, char_hash)</code> → 依模式分層把雜湊填進對應欄位。這張表就是兩種模式<b>共用的索引</b>；之後每批處理都查表，不再逐檔讀磁碟。',
      'help.h_incremental': '為什麼不用每次全表掃描',
      'help.list_incremental': '<li>scan 用 <code>INSERT OR IGNORE</code> 只補新卡、<code>prune</code> 清掉消失的卡，既有列不動。</li><li>每層雜湊的候選 SELECT 都過濾 <code>hash IS NULL</code> → 只算還沒算的。</li><li>兩種模式用<b>各自獨立的欄位</b>（位元＝head/full、進階＝char_len/char_hash）→ 換模式重用同一批列，只補那一欄。</li><li>長同步每約 <code>10s</code> commit 一次，中途關掉下次靠 <code>hash IS NULL</code> 接著跑。</li><li>想強制整池重算就勾「重建索引」清表重建（只有同路徑被<b>原地改內容</b>時才需要，因為 <code>INSERT OR IGNORE</code> 不會更新既有列）。</li>',
      'help.h_subfolder': '會掃子資料夾嗎？',
      'help.p_subfolder': '<b>不會，只掃頂層。</b>子資料夾一律忽略（含工具自己的輸出）—— 讓正式卡池維持扁平、範圍可控。',
      'help.h_delete': '刪除怎麼處理',
      'help.p_delete': '先試 OS 回收桶（<code>trash</code> ／ Windows <code>IFileOperation</code>），<b>刪完驗證檔案真的不見了</b>才算數；網路磁碟沒有回收桶、shell 可能假報成功 → 偵測到還在就 <code>fs::remove_file</code> 硬刪，交給 NAS 自身的回收桶／版本控制保留可復原性。同時把該列從索引表移除。',
      'help.retour': '重看所有導覽圈',
      'help.close': '確認',

      'tour.dismiss': '這頁知道了 ✕',
      'tour.sync.db': '會在這份檔案內建立卡片清單，之後都用這份清單比對檔案',
      'tour.sync.rebuild': '重新建立整份清單：比較花時間，但能確保清單跟資料夾現況完全一致',
      'tour.sync.start': '開始掃描資料夾、建立卡片清單 —— 之後找重複都用這份清單',
      'tour.setup.modes': '用上一步建好的清單來挑檔案。換成還沒整理過的方式 → 清單還沒建，要先回上一步整理一次（免勾「重建索引」）',
      'tour.review.auto': '自動幫每組留最新的一張、其餘列成清單；你確認後才會刪（可復原）',
      'tour.summary.delete': '按下才真的刪 —— 本機進系統回收桶、NAS 進 NAS 回收桶，都救得回來',
    },
    // Other languages added in Tasks 5–10. zh-Hant is the only entry for now.
  };

  g.I18N = I18N; g.LANGS = LANGS; g.PLURAL_KEYS = PLURAL_KEYS;
  g.detectLang = detectLang; g.setLang = setLang; g.getLang = getLang;
  g.t = t; g.pluralCategory = pluralCategory;
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: Run runtime test to verify it passes**

Run: `node scripts/i18n.test.mjs`
Expected: `i18n.test.mjs OK`

- [ ] **Step 5: Write the parity validator**

Create `scripts/check-i18n.mjs`:

```js
import { readFileSync } from 'node:fs';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const code = readFileSync(join(here, '..', 'dist', 'i18n.js'), 'utf8');
const sb = {};
vm.createContext(sb);
vm.runInContext(code, sb);
const { I18N, LANGS, PLURAL_KEYS } = sb;

const REF = 'zh-Hant';
const refKeys = Object.keys(I18N[REF]);
let problems = 0;
const fail = (m) => { console.error('  ✗ ' + m); problems++; };

const isEmpty = (v) =>
  v == null ||
  (typeof v === 'string' && v.trim() === '') ||
  (typeof v === 'object' && Object.values(v).some((x) => String(x).trim() === ''));

for (const { code } of LANGS) {
  const d = I18N[code];
  if (!d) { fail(`language "${code}" missing entirely`); continue; }
  const keys = Object.keys(d);
  for (const k of refKeys) if (!(k in d)) fail(`[${code}] missing key: ${k}`);
  for (const k of keys) if (!(k in I18N[REF])) fail(`[${code}] extra key not in ${REF}: ${k}`);
  for (const k of keys) if (k in I18N[REF] && isEmpty(d[k])) fail(`[${code}] empty value: ${k}`);
  for (const k of PLURAL_KEYS) {
    const v = d[k];
    if (typeof v !== 'object') { fail(`[${code}] plural key not an object: ${k}`); continue; }
    if (code === 'ru') {
      for (const c of ['one', 'few', 'many']) if (!(c in v)) fail(`[ru] plural ${k} missing "${c}"`);
    } else if (code === 'en' || code === 'es') {
      for (const c of ['one', 'other']) if (!(c in v)) fail(`[${code}] plural ${k} missing "${c}"`);
    } else {
      if (!('other' in v)) fail(`[${code}] plural ${k} missing "other"`);
    }
  }
}

if (problems) { console.error(`\ni18n parity FAILED: ${problems} problem(s).`); process.exit(1); }
console.log(`i18n parity OK — ${LANGS.length} language(s), ${refKeys.length} keys each.`);
```

- [ ] **Step 6: Run parity to verify it passes (1 language)**

Run: `node scripts/check-i18n.mjs`
Expected: `i18n parity OK — 7 language(s), <N> keys each.`
Note: `LANGS` lists 7 but `I18N` only has `zh-Hant` → the loop will FAIL with "language X missing entirely" for the other 6. **This is expected at this point.** To keep Task 1 green in isolation, temporarily verify only with: the test in Step 4 (the runtime). The parity script will pass fully only after Task 10. Document this: until all languages exist, `check-i18n.mjs` is expected to report the not-yet-added languages as missing.

- [ ] **Step 7: Commit**

```bash
git add dist/i18n.js scripts/i18n.test.mjs scripts/check-i18n.mjs
git commit -m "i18n: zero-dep runtime + zh-Hant reference dictionary + test harness"
```

---

## Task 2: Wire index.html static markup + language switcher + init

**Files:**
- Modify: `dist/index.html`

**Interfaces:**
- Consumes from Task 1: `t`, `setLang`, `getLang`, `detectLang`, `LANGS` (globals).
- Produces: `applyI18n()` (re-applies all static + dynamic chrome on language change); a populated `#langSel` switcher.

**Goal of this task:** after it, the app looks and behaves *exactly as today* in zh-Hant, but every static string flows through `t()` and the switcher exists (only zh-Hant has content, so other options render English-fallback=key until later tasks — that's fine, verified visually in zh-Hant).

- [ ] **Step 1: Load i18n.js before the main script**

In `dist/index.html`, immediately before the existing `<script>` (line ~300, the `// ---------- mock 卡片圖 ...` one), add:

```html
<script src="i18n.js"></script>
```

- [ ] **Step 2: Set the active language at the very top of the main script**

As the first statements inside the existing main `<script>` (before `const NAMES=...`):

```js
// i18n: pick language before any rendering runs
setLang(localStorage.getItem('dedupe.lang') || detectLang(navigator.language || 'en'));
```

- [ ] **Step 3: Add the switcher to the header + CSS**

In `<style>`, add near the `.stepper` rules:

```css
.langsel{margin-left:10px;background:var(--panel2);color:var(--txt);border:1px solid var(--line);border-radius:8px;padding:5px 8px;font:inherit;font-size:13px;cursor:pointer}
.langsel:hover{border-color:var(--accent)}
```

In `<header>`, change the `<h1>` and add the `<select>` after the `.stepper` div:

```html
<h1>🃏 <span data-i18n="app.title">卡片去重</span> <button class="help" id="helpBtn" data-i18n-title="help.btn_title" title="這是什麼？怎麼運作？">?</button></h1>
```
```html
<select class="langsel" id="langSel" title="Language / 語言"></select>
```
(place the `<select>` as the last child of `<header>`, after `<div class="stepper">…</div>`.)

- [ ] **Step 4: Add `data-i18n*` attributes to every static node**

Apply this mapping (attribute → key) to the existing nodes. Use `data-i18n` for textContent, `data-i18n-html` for innerHTML (markup-bearing), `data-i18n-placeholder`, `data-i18n-title`. Computed/rendered nodes are intentionally **excluded** (handled in Task 3).

Stepper: `<span data-step="1" data-i18n="step.sync">`, `step="2" → step.setup`, `step="3" → step.review`, `step="4" → step.delete`.

v-sync:
- `h2` → `data-i18n="sync.title"`
- `p.sub` → `sync.sub`
- label 卡片資料夾 → `sync.label.root`; `#btnBrowseRoot` → `sync.browse`
- label 索引檔 → `sync.label.db`; `#btnBrowseDb` → `sync.pick_loc`
- label 遊戲路徑 → `sync.label.game`; `#cfgGame` → `data-i18n-placeholder="sync.game_ph"`; `#btnBrowseGame` → `sync.pick_folder`
- label 找重複的方式 → `sync.label.mode`
- radio label 完全一樣的檔案 → `mode.byte`; radio label 同角色、不同封面 → `mode.char`
- label 重建索引 → `sync.label.rebuild`; rebuild text 重新建立整份清單 → `sync.rebuild_opt` (wrap the text after the checkbox in a `<span data-i18n="sync.rebuild_opt">`)
- `#syncPhase` → `data-i18n="sync.idle"`; `#syncLog` → `data-i18n="sync.log_start"`
- `#btnSync` → `sync.start`; `#btnSyncSkip` → `sync.skip`
- `#rootStatus` → leave as-is (computed in Task 3 init)

v-setup:
- `h2` → `setup.title`; `p.sub` → `setup.sub`
- mode byte `<b>` → `mode.byte`; its `<small>` → `setup.byte_desc`
- mode char `<b>` → `mode.char` (keep the badge span separate); badge 無視封面 → `setup.badge_ignore_cover`; its `<small>` → `setup.char_desc`
- batch label 一次看幾組 → `setup.batch_label`
- `#availTxt` → leave as-is (computed in Task 3)
- `#btnBackSync` → `common.back`; `#btnStart` → `setup.start`

v-review (only static chrome; rendered text in Task 3):
- `#grpHint` → `data-i18n-html="review.hint"`
- `#btnSelOthers` → `review.select_others`; `#btnDetail` → `review.detail`; `#btnAuto` → `review.auto`
- `#btnPrev` → `review.prev`; `#btnNext` → `review.next`
- `#grpCount`, `#modeBadge`, `#grpTitle`, `#selInfo` → leave (rendered in Task 3)

v-summary:
- `h2` → `summary.title`; `p.sub` → `summary.sub`
- `#sumWarn` → `data-i18n-html="summary.warn"` (keep its inline style attrs)
- table headers 組/檔名/大小 → `summary.col_group` / `summary.col_name` / `summary.col_size`
- `#btnDelete` → `summary.delete`; `#btnBackReview` → `summary.back`
- the `.total` div: replace its inner markup with `<div class="total" id="sumTotal"></div>` (rendered in Task 3)

How-it-works modal:
- `h3`: `<h3><span data-i18n="help.title">How it works</span> <span style="color:var(--dim);font-size:14px;font-weight:400" data-i18n="help.subtitle">· 技術細節</span></h3>`
- `p.lead` → `data-i18n-html="help.lead"`
- each `h4` → `help.h_normal` / `help.h_advanced` / `help.h_build` / `help.h_incremental` / `help.h_subfolder` / `help.h_delete` (in document order)
- each `p` under them → `data-i18n-html` `help.p_normal` / `help.p_advanced` / `help.p_build` / `help.p_subfolder` / `help.p_delete`
- the `<ul>` → `data-i18n-html="help.list_incremental"`
- `#reTour` → `help.retour`; `#closeHelp` → `help.close`

Tour: `#tourDismiss` → `data-i18n="tour.dismiss"`.

- [ ] **Step 5: Implement `applyI18n()` + switcher wiring at the end of the script**

Just before the final `drawTour();` call at the end of the script, add:

```js
// ---------- i18n apply / switcher ----------
function applyStatic(){
  document.querySelectorAll('[data-i18n]').forEach(el=>{ el.textContent = t(el.dataset.i18n); });
  document.querySelectorAll('[data-i18n-html]').forEach(el=>{ el.innerHTML = t(el.dataset.i18nHtml); });
  document.querySelectorAll('[data-i18n-placeholder]').forEach(el=>{ el.placeholder = t(el.dataset.i18nPlaceholder); });
  document.querySelectorAll('[data-i18n-title]').forEach(el=>{ el.title = t(el.dataset.i18nTitle); });
}
function buildLangSwitcher(){
  const sel=document.getElementById('langSel');
  sel.innerHTML = LANGS.map(l=>`<option value="${l.code}">${l.native}</option>`).join('');
  sel.value = getLang();
  sel.onchange = ()=>{ setLang(sel.value); localStorage.setItem('dedupe.lang', getLang()); applyI18n(); };
}
function setWindowTitle(){
  const title = t('app.title');
  document.title = title;
  try{ if(window.__TAURI__) window.__TAURI__.window.getCurrentWindow().setTitle(title); }catch(e){}
}
function applyI18n(){
  applyStatic();
  document.documentElement.lang = getLang();
  setWindowTitle();
  renderRecent(loadCfg().recent);      // re-localize "最近："
  setMode(MODE);                       // re-localize availTxt (+ Tauri refreshAvail via modeHooks)
  if(document.getElementById('v-sync').classList.contains('on')){
    // refresh idle/log chrome handled by applyStatic; nothing else needed
  }
  if(document.getElementById('v-review').classList.contains('on')) showGroup();
  if(document.getElementById('v-summary').classList.contains('on')) buildSummary();
  drawTour();
}
buildLangSwitcher();
applyI18n();
```

Note: `applyI18n()` replaces the old trailing `drawTour();` (drawTour is now called inside it). Remove the standalone final `drawTour();`.

- [ ] **Step 6: Manual smoke (zh-Hant unchanged)**

Open `dist/index.html` in a browser (file://). Expected: identical to before — all Chinese text present, switcher shows 7 native names with `繁體中文` selected, no raw `key` strings, no console errors. Switching to e.g. `English` now shows keys/Chinese fallback for un-added languages (expected until Task 5).

- [ ] **Step 7: Commit**

```bash
git add dist/index.html
git commit -m "i18n: route static UI + header switcher through t() (zh-Hant parity)"
```

---

## Task 3: Route dynamic JS strings through t()

**Files:**
- Modify: `dist/index.html` (script section)

**Interfaces:**
- Consumes: `t` and the keys from Task 1.

Replace each literal below (exact old → new). Line numbers are approximate (pre-edit).

- [ ] **Step 1: SYNC dynamic + PHASE map**

`PHASE` object → use `t`:
```js
const PHASE={scan:t('phase.scan'),head:t('phase.compare'),full:t('phase.compare'),'char-scan':t('phase.read_char'),char:t('phase.read_char')};
```
(Move `PHASE` construction so it is rebuilt per use, OR rebuild inside the listener. Simplest: inside the `sync-progress` listener, replace `const name=PHASE[p.phase]||p.phase;` with a local map built from `t()` each event.)

`refreshRootStatus`: replace the `s.textContent=...` with:
```js
s.textContent='…'; s.className='cfg-status'; // computing
s.textContent=t('sync.computing');
// after invoke:
s.className='cfg-status '+(n>0?'ok':'bad');
s.textContent = n>0 ? t('sync.root_ok',{n}) : t('sync.root_bad',{n});
```

`btnSync` Tauri handler:
- `if(!cfgRoot.value){ toast('先選卡片資料夾'); return; }` → `toast(t('toast.pick_root_first'))`
- `ph.textContent=\`開始整理${full?'（重建整份清單）':''}…\`;` → `ph.textContent=t('sync.starting',{full: full?t('sync.full_note'):''});`
- `log.textContent='開始整理…\n';` → `log.textContent=t('sync.log_starting')+'\n';`
- success log line → `log.textContent+=t('sync.log_done',{total:s.total,groups:s.groups,dup:s.dup_files,added:s.new,pruned:s.pruned})+'\n';`
- `ph.textContent='✓ 整理完成';` → `t('sync.done')`
- error: `log.textContent+='錯誤：'+e+'\n';` → `log.textContent+=t('sync.log_error')+e+'\n';`; `ph.textContent='✗ 出錯了';` → `t('sync.error')`

- [ ] **Step 2: SETUP availLabel + refreshAvail**

`availLabel()` (mock): replace returns with `t('setup.avail',{n: REAL?REAL.length:14293})` (drop mock parentheticals; mock numbers retained).

`refreshAvail()` (Tauri):
```js
const label=MODE==='char'?t('mode.char_short'):t('mode.byte_short');
el.textContent = s.synced ? t('setup.avail',{n:s.groups}) : t('setup.avail_not_synced',{label});
// catch: el.textContent=t('setup.avail_none');
```

- [ ] **Step 3: REVIEW renderGroup + copyCard**

In `renderGroup()`:
- `document.getElementById('grpCount').textContent=\`第 ${gi+1} 組 / 共 ${BATCH.length} 組\`;` → `t('review.group_count',{i:gi+1,n:BATCH.length})`
- `mb.textContent=adv?'同一個角色 · 封面不同':'完全一樣的檔案';` → `adv?t('review.badge_char'):t('mode.byte')`
- title: `adv ? \`這幾張其實是同一個角色\` : \`這幾張是一模一樣的檔案\`` → `adv?t('review.title_char'):t('review.title_byte')`
- card button labels in the template: `${f.copied?'已複製 ✓':'複製到遊戲目錄'}` → `${f.copied?t('review.copied_btn'):t('review.copy_to_game')}`; `<div class="copied">✓ 已複製</div>` → `t('review.copied')`
- `document.getElementById('selInfo').textContent=\`已選 ${g.selected.size} 張要刪\`;` → `t('review.sel_info',{n:g.selected.size})`

In `copyCard()`:
- `toast('複製到遊戲目錄僅桌面版可用')` → `toast(t('toast.copy_desktop_only'))`
- `toast('先在步驟1填「遊戲 chara 路徑」')` → `toast(t('toast.set_game_path'))`
- `toast('此卡無來源路徑')` → `toast(t('toast.no_src_path'))`
- `toast('已複製到遊戲目錄 ✓')` → `toast(t('toast.copied_ok'))`
- button `btn.textContent='已複製 ✓';` → `t('review.copied_btn')`; appended badge `b.textContent='✓ 已複製';` → `t('review.copied')`
- `catch(e){ toast('複製失敗：'+e, true); }` → `toast(t('toast.copy_fail',{e:mapErr(e)}), true)` where `mapErr` is added in Task 4.

- [ ] **Step 4: DETAIL strings**

- `f.strings=['(讀取失敗) '+e];` → `[t('detail.read_fail')+e]`
- loading bar `'<div class="detail-bar">載入角色資料中…</div>'` → `'<div class="detail-bar">'+t('detail.loading')+'</div>'`
- rescan button `▶ 逐行比對` → `t('detail.rescan')`
- legend `相同`/`不同` → `t('detail.same')` / `t('detail.diff')`
- tally: `\`<b style="color:var(--good)">${same}</b> 列相同 · <b style="color:var(--hl)">${diff}</b> 列不同\`` →
  ```js
  t.dummy; // (illustrative) compose:
  `<b style="color:var(--good)">${same}</b> ${t('detail.tally_same',{same:'',n:same}).trim()} · <b style="color:var(--hl)">${diff}</b> ${t('detail.tally_diff',{diff:'',n:diff}).trim()}`
  ```
  Implementation note: `detail.tally_same` value is `'{same} 列相同'`; calling with `same:''` yields ` 列相同` then `.trim()` → `列相同`, keeping the bold number separate. For languages where the number must be inline, the `{same}` placeholder simply renders empty and the unit word remains — acceptable. (Alternative kept simple: render the full localized phrase without separate bold; either is acceptable — prefer the inline `t('detail.tally_same',{same, n:same})` for natural word order.) **Chosen: inline** —
  ```js
  const t1=t('detail.tally_same',{same, n:same}), t2=t('detail.tally_diff',{diff, n:diff});
  if(tt) tt.innerHTML = `<span style="color:var(--good)">${t1}</span> · <span style="color:var(--hl)">${t2}</span>`;
  ```

- [ ] **Step 5: SUMMARY render + delete**

`buildSummary()`:
- after computing `count`/`bytes`, set the total node:
  ```js
  document.getElementById('sumTotal').innerHTML = t('summary.total',{count, size:fmtMB(bytes), n:count});
  ```
  (remove the old `sumCount`/`sumSize` textContent writes.)

mock `btnDelete` handler:
- `const n=document.getElementById('sumCount').textContent;` → derive from BATCH or store count; replace with:
  ```js
  let count=0; BATCH.forEach(g=>count+=g.selected.size);
  toast(t('toast.deleted',{n:count}));
  ```

Tauri `btnDelete`:
- `if(!names.length){ toast('沒有選取任何卡'); return; }` → `toast(t('toast.none_selected'))`
- success: `let msg=\`已刪除 ${res.deleted} 張（${(res.freed/1048576).toFixed(2)} MB）\`;` → `let msg=t('toast.deleted_size',{n:res.deleted, mb:(res.freed/1048576).toFixed(2)});`
- `if(res.errors&&res.errors.length) msg+=\`（${res.errors.length} 個錯誤）\`;` → `msg+=t('toast.delete_errors',{n:res.errors.length});`
- `catch(e){ toast('刪除失敗：'+e, true); }` → `toast(t('toast.delete_fail',{e}), true)`

Tauri `btnStart` + `autoRun`:
- `toast('讀取失敗：'+e, true)` → `toast(t('toast.read_fail',{e}), true)`
- `toast('找不到重複的卡，先回上一步「整理卡片」跑一次')` → `toast(t('toast.no_dups'))`

mock `btnAuto`: `toast('全部自動：桌面版功能')` → `toast(t('toast.auto_desktop_only'))`

- [ ] **Step 6: TOUR ANN built from t()**

Replace the static `const ANN={...}` with a function rebuilt on each draw. In `drawTour()`, before computing `list`, build annotations from `t()`:

```js
const ANN={
  sync:[ {sel:'#cfgDb',text:t('tour.sync.db'),side:'bottom',color:'#ffb454'},
         {sel:'#rebuildOpt',text:t('tour.sync.rebuild'),side:'bottom',color:'#7ee0a0'},
         {sel:'#btnSync',text:t('tour.sync.start'),side:'bottom',color:'#ff8a9b'} ],
  setup:[ {sel:'#modeCards',text:t('tour.setup.modes'),side:'bottom',color:'#ff8a9b'} ],
  review:[ {sel:'#btnAuto',text:t('tour.review.auto'),side:'bottom',color:'#ffd23f'} ],
  summary:[ {sel:'#btnDelete',text:t('tour.summary.delete'),side:'bottom',color:'#ff8a9b'} ],
};
```
(Move the `ANN` definition inside `drawTour()` so it re-localizes; delete the old top-level `const ANN`.)

- [ ] **Step 7: Manual smoke (zh-Hant unchanged, dynamic paths)**

Open `dist/index.html` (file://). Walk all 4 steps: sync (mock progress), setup (avail label), review (group counter, select info, detail compare tally, copy button), summary (total line). Expected: identical Chinese text as before; no raw `{n}` or `key` leaks; no console errors.

- [ ] **Step 8: Commit**

```bash
git add dist/index.html
git commit -m "i18n: route all dynamic strings, toasts, tour through t()"
```

---

## Task 4: Backend error codes + window-title permission + neutral title

**Files:**
- Modify: `src-tauri/src/lib.rs:74,77`
- Modify: `src-tauri/capabilities/default.json`
- Modify: `src-tauri/tauri.conf.json`
- Modify: `dist/index.html` (add `mapErr` helper)

**Interfaces:**
- Produces: `copy_to_game` now returns error strings `"src_no_name"` and `"game_path_invalid|<path>"`. Frontend `mapErr(e)` maps these to localized text.

- [ ] **Step 1: Change the two Rust error literals to codes**

In `src-tauri/src/lib.rs`:
```rust
let name = src.file_name().ok_or_else(|| "src_no_name".to_string())?;
let dest = PathBuf::from(&dest_dir);
if !dest.is_dir() {
    return Err(format!("game_path_invalid|{}", dest.display()));
}
```

- [ ] **Step 2: Verify Rust still builds & tests pass**

Run: `cd src-tauri && cargo test`
Expected: builds; existing tests PASS (no test asserts on these strings).

- [ ] **Step 3: Add the window set-title permission**

In `src-tauri/capabilities/default.json`, add to `permissions`:
```json
"permissions": [
  "core:default",
  "core:window:allow-set-title",
  "dialog:default"
]
```

- [ ] **Step 4: Neutral fallback window title**

In `src-tauri/tauri.conf.json`, change `app.windows[0].title` from `"卡片去重"` to `"Koikatsu Dedupe"`.

- [ ] **Step 5: Add `mapErr` helper + use it in copyCard**

In `dist/index.html` script (near `toast`), add:
```js
function mapErr(e){
  const s=String(e&&e.message||e);
  if(s==='src_no_name') return t('err.src_no_name');
  if(s.startsWith('game_path_invalid|')) return t('err.game_path_invalid',{path:s.slice('game_path_invalid|'.length)});
  return s;
}
```
Ensure `copyCard`'s catch (Task 3 Step 3) uses `t('toast.copy_fail',{e:mapErr(e)})`.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/lib.rs src-tauri/capabilities/default.json src-tauri/tauri.conf.json dist/index.html
git commit -m "i18n: backend error codes localized in frontend; dynamic window title"
```

---

## Tasks 5–10: Add translations (one language per task)

**Each task adds one language object to `I18N` in `dist/i18n.js`, translating ALL keys from the `zh-Hant` reference, consistent with the glossary (spec Appendix B). The gate is the parity validator + a manual smoke in that language.**

**Shared procedure for each language `<code>`:**

1. **Add the dictionary.** Copy the full `zh-Hant` key set; translate each value. Rules:
   - Preserve all interpolation placeholders verbatim: `{n} {i} {total} {groups} {dup} {added} {pruned} {label} {same} {diff} {count} {size} {mb} {e} {path} {full}`.
   - Preserve HTML/markup inside `data-i18n-html` values (`<b> <code> <li>`), translating only human text — code identifiers (`xxHash64`, `char_len`, `IEND`, `INSERT OR IGNORE`, SQL) stay literal.
   - Keep glyphs/arrows/emoji where they are language-neutral (`→ ← ✓ ✗ ⚡ ⚠ ▶ ·`).
   - For the 8 `PLURAL_KEYS`, provide a plural **object** with the categories required for `<code>` (see Global Constraints).
2. **Run parity** focused on this language: `node scripts/check-i18n.mjs` — must report no problems for `<code>` (other not-yet-added languages may still fail until Task 10; confirm zero `[<code>]` lines).
3. **Manual smoke:** open `dist/index.html`, switch to `<code>`, walk all 4 steps + open "How it works" + trigger a toast. No raw keys, no `{placeholder}` leaks, layout holds (watch long ru/de-style strings on buttons).
4. **Commit:** `git add dist/i18n.js && git commit -m "i18n: add <native-name> (<code>) translations"`.

- [ ] **Task 5 — `en` (English).** Plurals `one/other`. Examples: `'sync.start':'Start'`, `'review.next':'Next →'`, `'review.sel_info':{one:'{n} card marked for deletion', other:'{n} cards marked for deletion'}`, `'summary.delete':'Confirm delete'`, `'toast.no_dups':'No duplicates found — go back and run "Sync" once.'`, `'help.title':'How it works'`.
- [ ] **Task 6 — `zh-Hans` (简体中文).** Plurals `other` only. Mostly Hant→Hans conversion + term localization (索引檔→索引文件, 資料夾→文件夹, 回收桶→回收站, 卡片→卡片). e.g. `'sync.title':'整理卡片（建一份清单）'`, `'summary.col_name':'文件名'`.
- [ ] **Task 7 — `ja` (日本語).** Plurals `other` only. e.g. `'sync.start':'整理を開始'`, `'review.next':'次のグループ →'`, `'summary.delete':'削除を確定'`, `'mode.byte':'完全に同じファイル'`, `'mode.char':'同じキャラ・違う表紙'`.
- [ ] **Task 8 — `ko` (한국어).** Plurals `other` only. e.g. `'sync.start':'정리 시작'`, `'review.next':'다음 그룹 →'`, `'summary.delete':'삭제 확인'`.
- [ ] **Task 9 — `ru` (Русский).** Plurals `one/few/many`. e.g. `'review.sel_info':{one:'Выбрана {n} карта для удаления', few:'Выбрано {n} карты для удаления', many:'Выбрано {n} карт для удаления'}`, `'summary.delete':'Подтвердить удаление'`. Watch button width.
- [ ] **Task 10 — `es` (Español).** Plurals `one/other`. e.g. `'review.sel_info':{one:'{n} tarjeta marcada para borrar', other:'{n} tarjetas marcadas para borrar'}`, `'summary.delete':'Confirmar borrado'`.

**After Task 10:** `node scripts/check-i18n.mjs` MUST print `i18n parity OK — 7 language(s), <N> keys each.` (zero problems). `node scripts/i18n.test.mjs` MUST print OK.

---

## Task 11: Cross-language QA + final verification + docs

**Files:**
- Modify (as QA finds): `dist/i18n.js`
- Modify: `README.md` (one-line i18n note)

- [ ] **Step 1: Parity + runtime gates green**

Run: `node scripts/check-i18n.mjs && node scripts/i18n.test.mjs`
Expected: `i18n parity OK — 7 language(s), …` and `i18n.test.mjs OK`.

- [ ] **Step 2: Native-level QA review (parallel)**

Dispatch one reviewer per non-reference language (en, zh-Hans, ja, ko, ru, es). Each checks its dictionary object against the `zh-Hant` reference for: naturalness, glossary consistency (Appendix B), correct plural forms, placeholder preservation, and UI-length sanity (buttons/badges). Apply fixes to `dist/i18n.js`. Re-run Step 1.

- [ ] **Step 3: Full 7-language smoke**

For each of the 7 languages: switch via the header `<select>`, walk sync→setup→review→summary, open "How it works", trigger at least one toast, confirm the guided-tour crayon labels are localized, and confirm the OS window title updates (Tauri build). Confirm choice persists across reload (`localStorage['dedupe.lang']`). Confirm `file://` mock still works.

- [ ] **Step 4: Rust regression**

Run: `cd src-tauri && cargo test`
Expected: PASS.

- [ ] **Step 5: README note + commit**

Add to `README.md` (near the UI paragraph): a sentence that the UI ships in 7 languages (繁中/简中/EN/JA/KO/RU/ES), auto-detected on first run and switchable top-right; translations are community-reviewable in `dist/i18n.js`. Document the check: `node scripts/check-i18n.mjs`.

```bash
git add dist/i18n.js README.md
git commit -m "i18n: cross-language QA fixes + docs"
```

---

## Self-Review (filled)

**Spec coverage:**
- 7 languages + native names → Tasks 1,5–10. ✓
- Detection + en fallback + persistence → Task 1 (`detectLang`), Task 2 (init/switcher). ✓
- `dist/i18n.js` zero-dep, no-build → Task 1. ✓
- `data-i18n*` static + dynamic `t()` + switcher + window title + html lang → Tasks 2,3. ✓
- Plurals (ru 3-form, en/es) → Task 1 runtime + Tasks 9,10. ✓
- Backend error codes + capability + neutral title → Task 4. ✓
- Parity test + cargo green + manual smoke → Tasks 1,11. ✓
- Out-of-scope (mock numbers, core.rs, std errors) → respected (Task 3 keeps mock numbers; no core.rs edits). ✓
- Glossary consistency → Tasks 5–10 procedure + Task 11 QA. ✓

**Placeholder scan:** No TBD/TODO. Translation Tasks 5–10 are content-generation gated by the parity validator + smoke — concrete examples given per language; full content produced at execution (cannot pre-write 660 strings, and the verifiable gate is parity+QA). The `detail.tally` step documents the chosen inline approach.

**Type consistency:** `t/setLang/getLang/detectLang/LANGS/PLURAL_KEYS/pluralCategory` names match across Tasks 1–3. `mapErr` defined in Task 4, used in Task 3 Step 3 (note added). `applyI18n/applyStatic/buildLangSwitcher/setWindowTitle` consistent within Task 2. Plural param convention (`params.n`) consistent across Task 3 call sites.
